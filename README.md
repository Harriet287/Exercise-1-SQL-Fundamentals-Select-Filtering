# Exercise-1-SQL-Fundamentals-Select-Filtering
This project contains SQL exercises on basic queries, including SELECT statements, WHERE filtering, sorting results, and logical operators. It focuses on building a solid foundation in SQL syntax and query structure.
